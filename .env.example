# Make sure you change the file name from ".env.example" to ".env"

# Azure AD Authentication
TENANT_ID=your-azure-tenant-id 
CLIENT_ID=your-azure-client-id
CLIENT_SECRET=your-secure-client-secret
REDIRECT_URI=http://localhost:3000/auth/callback  # Update if different locally

# Optional Group Restriction
ALLOWED_GROUPS=group1_object_id,group2_object_id

# Application Settings
PORT=3000
SESSION_SECRET=a-very-long-and-random-secret-for-sessions
